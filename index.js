//arithmetic operators = operands (values, variables, etc.)
                        //  operators (+ - * /)
                        //  ex. 11 = x + 5

let students = 30;

// students = students - 1;
// students = students * 2;
// students = students / 2;
// students = students ** 2;
// students = students % 2;

// students += 1;
// students *= 2;

// students++;

console.log(students);